# Нађи први

Ово је пример у ком функција добија адресу друге функције као аргумент и онда ту функцију позива индиректно.

## Шта програм ради

Функција `nadji_prvi` пролази кроз низ и враћа индекс првог елемента који задовољава прослеђени predicate. Ако такав елемент не постоји, враћа `-1`.

Референтна C++ верзија могла би да изгледа овако:

```cpp
int nadji_prvi(unsigned* a, int n, int (*pred)(unsigned)) {
    for (int i = 0; i < n; i++) {
        if (pred(a[i])) {
            return i;
        }
    }
    return -1;
}
```

## Датотеке

- `main.cpp` учитава низ и позива `nadji_prvi` са две различите predicate функције
- `nadji_prvi.s` садржи асемблерску имплементацију функције

## Шта треба посматрати у асемблеру

На уласку у функцију трећи аргумент, односно адреса predicate функције, стиже у `rdx`. Пошто је то вредност која нам треба у свакој итерацији, смештамо је у локалну променљиву у стек оквиру:

```asm
mov qword ptr [rbp - 16], rdx
```

Исто радимо и са базом низа, дужином и текућим индексом. То омогућава да се после сваког `call` лако вратимо на стање петље.

Сам индиректан позив изгледа овако:

```asm
call qword ptr [rbp - 16]
```

То значи: не позивај симбол познат по имену, него адресу функције која је тренутно смештена у меморији на датој позицији стек оквира.

Ако predicate врати ненулту вредност, индекс одмах враћамо. Овде је важна и рана завршница: чим нађемо први погодак, петља се прекида.

## Превођење

```sh
g++ main.cpp nadji_prvi.s
```

## Покретање

```sh
./a.out
```

Пример интеракције:

```text
6
11 15 18 21 130 7
prvi paran: indeks 2
prvi > 99: indeks 4
```

## На шта треба обратити пажњу

- ово је први пример са индиректним `call`
- predicate и даље користи исту конвенцију позива као и свака друга функција: аргумент у `edi`, повратна вредност у `eax`
- `-1` је природан сигнал да ниједан елемент није задовољио услов

## Навигација

- Претходно: [Опсег низа](../02-opseg/README.md)
- Следеће: [Рекурзивни факторијел](../04-faktorijel_rekurzivno/README.md)
- Горе: [Недеља 5](../README.md)
